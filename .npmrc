# NPM Configuration for BaseKit

# Suppress audit warnings during install
# (Vulnerabilities are dev-only and documented in NPM_AUDIT_EXPLANATION.md)
audit=false

# Use legacy peer dependency resolution to avoid conflicts
legacy-peer-deps=true
